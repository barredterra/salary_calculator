[project]
name = "salary-calculator"
version = "0.1.0"
description = "Small Tkinter salary calculator with declining utility by hours worked"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
salary-calculator = "main:main"
